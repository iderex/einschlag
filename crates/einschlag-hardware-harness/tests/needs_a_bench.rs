//! Temporary. Introduced to watch the guard refuse it, reverted in the next commit.

#[test]
fn measure_a_perforation_at_the_bench() {
    panic!("this needs a bench and the default suite just started it");
}
